{
  "name": "anveshana-cms",
  "version": "1.0.0",
  "description": "Lightweight markdown-based CMS pipeline for Anveshana.net — write a Markdown file, run the build, get a fully updated static site.",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "node scripts/build.js",
    "watch": "node scripts/build.js --watch"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2"
  }
}
